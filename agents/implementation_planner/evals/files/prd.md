# slugify

Add a `slugify(text: str) -> str` helper that:

- lowercases
- replaces spaces with hyphens
- strips characters that are not letters, digits, or hyphens

Include unit tests. Do not add a CLI.
